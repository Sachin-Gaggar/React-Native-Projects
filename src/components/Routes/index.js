import React from "react";

import { NavigationContainer, StackActions } from "@react-navigation/native";
import TabContainer from "./TabScreen.js";

export default Routes = () => {
  return (
    <NavigationContainer>
      <TabContainer />
    </NavigationContainer>
  );
};
